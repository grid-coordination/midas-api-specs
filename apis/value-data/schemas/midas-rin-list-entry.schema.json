{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "midas-rin-list-entry.schema.json",
  "title": "MIDAS RIN List Entry",
  "description": "A RIN entry returned by the SignalType query.",
  "type": "object",
  "required": ["RateID", "SignalType", "Description"],
  "additionalProperties": false,
  "properties": {
    "RateID": {
      "type": "string",
      "pattern": "^[A-Z]{2}[A-Z]{2}-[A-Z0-9]{2}[A-Z0-9]{2}-[A-Z0-9]{4}-[A-Z0-9]{4,10}$",
      "description": "The Rate Identification Number (RIN)."
    },
    "SignalType": {
      "$ref": "midas-enums.schema.json#/$defs/signalType"
    },
    "Description": {
      "type": "string",
      "description": "Human-readable description including distribution and energy providers."
    },
    "LastUpdated": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of the last update to this RIN's data. Undocumented in upstream docs but present in live API responses."
    }
  }
}
