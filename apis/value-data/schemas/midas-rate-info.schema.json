{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "midas-rate-info.schema.json",
  "title": "MIDAS RateInfo",
  "description": "Rate information and associated time-series values for a single RIN.",
  "type": "object",
  "required": ["RateID", "ValueInformation"],
  "additionalProperties": false,
  "properties": {
    "RateID": {
      "type": ["string", "null"],
      "description": "The Rate Identification Number (RIN). Null when querying realtime on a non-realtime RIN."
    },
    "SystemTime_UTC": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "Server timestamp of the response in UTC."
    },
    "RateName": {
      "type": ["string", "null"],
      "description": "Name of the rate as provided by the uploading party."
    },
    "SignalType": {
      "type": ["string", "null"],
      "description": "Human-readable signal type, present in live v2.0 rate-values responses (confirmed 2026-06-22): 'Electricity Rates', 'Greenhouse Gas Emissions', or 'California Independent System Operator Flex Alert'. Matches the per-entry SignalType in the RIN list."
    },
    "Description": {
      "type": ["string", "null"],
      "description": "Human-readable description of the rate, present in live v2.0 rate-values responses (e.g. 'SGIP_CAISO_PGE Realtime GHG Emissions - Greenhouse Gas emissions')."
    },
    "RateType": {
      "type": ["string", "null"],
      "description": "Rate classification. Inconsistent wire format across signal types (confirmed live v2.0, 2026-06-22): electricity rates return the short Ratetype lookup UploadCode (TOU, CPP, RTP, VPP, DSR, V-D, C-D, R-D, T-D); SGIP GHG returns the long Description 'Greenhouse Gas emissions'; Flex Alert returns the long Description 'Flex Alert'. Match on both the UploadCode and the Description. May be null for some entries."
    },
    "Sector": {
      "type": ["string", "null"],
      "description": "Customer sector or null."
    },
    "EndUse": {
      "type": ["string", "null"],
      "description": "End use category or null."
    },
    "API_Url": {
      "type": ["string", "null"],
      "description": "API URL as provided by uploading party, or null."
    },
    "RatePlan_Url": {
      "type": ["string", "null"],
      "description": "Link to rate schedule as provided by uploading party, or null."
    },
    "AltRateName1": {
      "type": ["string", "null"],
      "description": "Alternate rate name, or null."
    },
    "AltRateName2": {
      "type": ["string", "null"],
      "description": "Another alternate rate name, or null."
    },
    "SignupCloseDate": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "Last day to sign up for the rate, or null. Live API returns full ISO 8601 datetime despite upstream docs describing it as a date."
    },
    "ValueInformation": {
      "type": ["array", "null"],
      "description": "Time-series collection of price/value intervals. Null when no data is available (e.g. realtime query on a non-realtime RIN).",
      "items": {
        "$ref": "midas-value-data.schema.json"
      }
    }
  }
}
